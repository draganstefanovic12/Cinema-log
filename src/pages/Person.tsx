import { Typography } from "@mui/material";
import { Container } from "@mui/system";

export const Person = () => {
  return (
    <Container>
      <Typography>test</Typography>
    </Container>
  );
};
